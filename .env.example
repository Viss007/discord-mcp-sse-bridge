DISCORD_TOKEN=
# Preferred direct channel target (ID):
DISCORD_CHANNEL_ID=
# Or fallback by server/channel name (both required if no ID):
DISCORD_SERVER_NAME=
DISCORD_CHANNEL_NAME=

SSE_URL=
MCP_HTTP_URL=
MCP_API_KEY=

# Send a one-time message on boot (optional)
NOTIFY_ON_START=false
NOTIFY_MESSAGE=Bridge deployed
